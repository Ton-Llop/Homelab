FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir uv

COPY pyproject.toml uv.lock README.md ./
COPY src ./src

RUN uv sync --frozen --no-dev

EXPOSE 8765

CMD ["uv", "run", "--no-sync", "uvicorn", "power_monitor.api:app", "--app-dir", "src", "--host", "0.0.0.0", "--port", "8765"]