services:
  power-monitor:
    build: .
    container_name: homelab-power-monitor
    restart: unless-stopped

    env_file:
      - ../.env

    environment:
    
      POWER_DB_PATH: /data/power.db

    volumes:
      - power-data:/data

    ports:
      - "8765:8765"

volumes:
  power-data:
